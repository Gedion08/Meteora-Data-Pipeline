apiVersion: v1
kind: Service
metadata:
  name: {{ include "meteora-pipeline.fullname" . }}
  labels:
    {{- include "meteora-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meteora-pipeline.selectorLabels" . | nindent 4 }}
